# Create the upgraded portfolio index.html and zip it for easy upload
import os, zipfile, textwrap, pathlib

html_content = """<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Anvay - Video Editing Portfolio</title>
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: Arial, sans-serif;
        scroll-behavior: smooth;
    }
    body {
        background-color: #0d0d0d;
        color: white;
        line-height: 1.6;
    }
    header {
        position: sticky;
        top: 0;
        background: rgba(0, 8, 20, 0.95);
        padding: 1rem 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        z-index: 100;
    }
    header h1 {
        color: #00b4d8;
    }
    nav a {
        color: white;
        text-decoration: none;
        margin-left: 1rem;
        transition: 0.3s;
    }
    nav a:hover {
        color: #00b4d8;
    }
    section {
        padding: 4rem 2rem;
        max-width: 1100px;
        margin: auto;
        opacity: 0;
        transform: translateY(50px);
        transition: all 0.8s ease-out;
    }
    section.show {
        opacity: 1;
        transform: translateY(0);
    }
    .hero {
        text-align: center;
        padding: 5rem 2rem;
        background: linear-gradient(180deg, #001d3d, #000814);
    }
    .hero h2 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        color: #00b4d8;
    }
    .hero p {
        font-size: 1.2rem;
        color: #90e0ef;
    }
    .about {
        display: flex;
        flex-wrap: wrap;
        gap: 2rem;
        align-items: center;
    }
    .about img {
        width: 300px;
        border-radius: 10px;
    }
    .about-text {
        flex: 1;
    }
    .portfolio {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
    }
    .portfolio video {
        width: 100%;
        border-radius: 8px;
        border: 2px solid #00b4d8;
        transition: transform 0.3s ease;
    }
    .portfolio video:hover {
        transform: scale(1.03);
    }
    .contact form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
    .contact input, .contact textarea {
        padding: 0.8rem;
        border: none;
        border-radius: 5px;
        background: #001d3d;
        color: white;
    }
    .contact button {
        padding: 0.8rem;
        border: none;
        background: #00b4d8;
        color: white;
        cursor: pointer;
        border-radius: 5px;
        transition: 0.3s;
    }
    .contact button:hover {
        background: #0096c7;
    }
    footer {
        text-align: center;
        padding: 1rem;
        background: #000814;
        color: #90e0ef;
    }
    @media(max-width: 768px) {
        .about {
            flex-direction: column;
            text-align: center;
        }
    }
</style>
</head>
<body>

<header>
    <h1>Anvay</h1>
    <nav>
        <a href="#about">About</a>
        <a href="#portfolio">Portfolio</a>
        <a href="#contact">Contact</a>
    </nav>
</header>

<section class="hero show">
    <h2>Crafting Stories Through Video</h2>
    <p>High-quality edits, cinematic cuts, and captivating storytelling</p>
</section>

<section id="about" class="about">
    <img src="https://via.placeholder.com/300" alt="Anvay Profile">
    <div class="about-text">
        <h2>About Me</h2>
        <p>I’m Anvay, a professional video editor with over 4 years of experience crafting compelling visuals. My work has helped clients achieve more than 100 million views across platforms. From cinematic storytelling to high-paced reels, I turn raw footage into engaging stories that connect with audiences.</p>
    </div>
</section>

<section id="portfolio">
    <h2 style="color:#00b4d8; text-align:center;">My Work</h2>
    <div class="portfolio">
        <video src="video1.mp4" muted loop></video>
        <video src="video2.mp4" muted loop></video>
        <video src="video3.mp4" muted loop></video>
    </div>
</section>

<section id="contact" class="contact">
    <h2 style="color:#00b4d8; text-align:center;">Contact Me</h2>
    <form onsubmit="return sendMessage(event)">
        <input type="text" placeholder="Your Name" required>
        <input type="email" placeholder="Your Email" required>
        <textarea rows="5" placeholder="Your Message" required></textarea>
        <button type="submit">Send</button>
    </form>
</section>

<footer>
    <p>© 2025 Anvay Suvidya Atre - Video Editing Portfolio</p>
</footer>

<script>
// Smooth fade-in when sections enter viewport
const sections = document.querySelectorAll("section");
const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add("show");
        }
    });
}, { threshold: 0.2 });
sections.forEach(section => observer.observe(section));

// Hover video preview
document.querySelectorAll(".portfolio video").forEach(video => {
    video.addEventListener("mouseenter", () => video.play());
    video.addEventListener("mouseleave", () => {
        video.pause();
        video.currentTime = 0; // Reset video
    });
});

// Contact form alert
function sendMessage(event) {
    event.preventDefault();
    alert("Thank you! Your message has been sent.");
}
</script>

</body>
</html>
"""

root = "/mnt/data/anvay_portfolio_site"
os.makedirs(root, exist_ok=True)
index_path = os.path.join(root, "index.html")
with open(index_path, "w", encoding="utf-8") as f:
    f.write(html_content)

# Create a zip
zip_path = "/mnt/data/anvay_portfolio_site.zip"
with zipfile.ZipFile(zip_path, "w", zipfile.ZIP_DEFLATED) as z:
    # ensure folder structure exists by writing index.html
    z.write(index_path, arcname="index.html")

(index_path, zip_path)
